<Window x:Class="BackupWpfClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backup Client" 
        Height="520" Width="860"
        Background="#F3F4F6">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0 0 0 20"
                   Foreground="#333"><Run Text="Backup"/><Run Language="ru-ru" Text=" "/><Run Text="Wpf"/></TextBlock>

        <!-- Карточка -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="12"
                Padding="20"
                BorderBrush="#DDD"
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="3"
                                  BlurRadius="10"
                                  Opacity="0.25"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Адрес сервера -->
                <StackPanel Orientation="Vertical"
                            Margin="0 0 0 15"
                            Grid.Row="0">
                    <TextBlock Text="Server address"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#555"/>
                    <TextBox x:Name="ServerAddressBox"
                             Height="32"
                             FontSize="14"
                             Margin="0 5 0 0"/>
                </StackPanel>

                <!-- Имя базы -->
                <StackPanel Orientation="Vertical"
                            Margin="0 0 0 15"
                            Grid.Row="1">
                    <TextBlock Text="Database name"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#555"/>
                    <TextBox x:Name="DatabaseNameBox"
                             Height="32"
                             FontSize="14"
                             Margin="0 5 0 0"
                             Text="backup_123"/>
                </StackPanel>

                <!-- Кнопка -->
                <Button Grid.Row="2"
                        Content="Run backup"
                        Width="160"
                        Height="40"
                        Margin="0 0 0 15"
                        FontSize="15"
                        Background="#4F8EF7"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        HorizontalAlignment="Left"
                        Click="RunBackup_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Background" Value="#4F8EF7"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3A73D8"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#2F61BC"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Статус -->
                <TextBlock Grid.Row="3"
                           x:Name="StatusText"
                           Text="Waiting..."
                           FontWeight="Bold"
                           Foreground="#666"
                           Margin="0 0 0 10"/>

                <!-- Логи -->
                <TextBox x:Name="LogBox"
                         Grid.Row="4"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         IsReadOnly="True"
                         Background="#FAFAFA"
                         BorderBrush="#CCC"
                         BorderThickness="1"
                         Padding="10"
                         FontFamily="Consolas"
                         FontSize="13"/>
            </Grid>
        </Border>
    </Grid>
</Window>
